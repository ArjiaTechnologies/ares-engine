# Public OHLCV ingestion through CCXT does not require exchange keys.
# Leave credentials unset unless you add authenticated, read-only endpoints yourself.
COINBASE_API_KEY=
COINBASE_API_SECRET=

# ARES ships without order execution. Never commit secrets.
